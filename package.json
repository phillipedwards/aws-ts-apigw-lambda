{
    "name": "aws-ts-apigw-lambda",
    "scripts": {
        "test-api": "curl -H 'Content-Type: application/json' -d '{ \"key\": \"value\" }' -XPOST $(pulumi stack output messageEndpoint)"
    },
    "devDependencies": {
        "@types/aws-lambda": "^8.10.83",
        "@types/node": "^10.0.0"
    },
    "dependencies": {
        "@pulumi/pulumi": "^3.0.0",
        "@pulumi/aws": "^4.0.0",
        "@pulumi/awsx": "^0.30.0"
    }
}
